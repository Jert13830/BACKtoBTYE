<section>
	<div class="table-container">
		<table>
			<thead>
				<tr>
					<th>Role</th>
					<th class="role-column">Action</th>
				</tr>
			</thead>
			<tbody>
				{% for role in roles%}
					<tr class="roleLine">
						<td>{{role.role}}</td>
						<td class="role-column">
							<form method="post" action="/roleList">
								<button type="button" class="btnForm" data-role-id="{{ role.id_role }}" data-role-name="{{ role.role }}" onclick="updateRole(this)">
									✏️
								</button>

								<button class="btnForm" id="btnDelete" name="buttons" value="delete-{{role.id_role}}">🗑️</button>
							</form>
						</td>
					</tr>
				{% endfor %}
			</tbody>
		</table>

	</div>
	<div id="addDiv">
		<form id="newRoleForm" name="newRoleForm" method="post" action="/postRole">
			<div>
				<label for="userRoleTitle">Role :
				</label>


				<input type="text" id="userRoleTitle" name="userRoleTitle" required minlength="3" maxlength="30" pattern="[a-z][a-z0-9_-]*">
				<input type="text" hidden id="updateId" value="">
				<button class="btnForm" id="btnAdd" name="btnAdd">Add</button>
				<button type="button" class="btnForm" onclick="window.location.href='/register'">
					Close
				</button>
			</div>
			{% if errors.userRoleTitle %}
				<p>{{ errors.userRoleTitle }}
				</p>
			{% endif %}
		</form>
	</div>
</section>
